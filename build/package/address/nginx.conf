events { }

http {
    server {
        listen 8080;

        location /api/address {
            return 200 '{"message": "Dirección mock"}';
        }

        location / {
            proxy_pass http://web:8081;
        }
    }
}
